{
  "name": "first-tree",
  "version": "0.3.1-alpha",
  "description": "CLI for initializing and maintaining first-tree context trees.",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "bin": {
    "first-tree": "./dist/index.js",
    "ft": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsdown",
    "prepack": "pnpm run build && node scripts/copy-auto-assets.mjs",
    "typecheck": "tsc -b",
    "test": "pnpm run build && vitest run",
    "test:coverage": "pnpm run build && vitest run --coverage"
  },
  "dependencies": {
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@first-tree/auto": "workspace:*",
    "@types/node": "^22.19.17",
    "@vitest/coverage-v8": "4.1.5",
    "tsdown": "^0.12.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  },
  "engines": {
    "node": ">=22"
  }
}
