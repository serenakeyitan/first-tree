{
  "name": "first-tree-monorepo",
  "version": "0.3.1-alpha",
  "private": true,
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "tsc -b",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "lint": "oxlint .",
    "fmt": "oxfmt . --write",
    "fmt:check": "oxfmt . --check",
    "precommit": "pnpm test && pnpm lint-staged",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "oxfmt": "^0.46.0",
    "oxlint": "^1.61.0",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.32.1"
}
