# `@first-tree/auto` assets

Runtime assets bundled with the package for the auto product surface.

Currently contains:

- `dashboard.html` — the SSE dashboard served by the auto daemon HTTP
  server (see `src/daemon/http.ts`).

All auto logic — CLI commands, daemon, broker, dispatcher, statusline —
is implemented in TypeScript under `src/`.
